// Тесты для AuthenticationManager
#include <UnitTest++/UnitTest++.h>
// Проверка создания объектов

SUITE(AuthenticationManagerTest) {
    TEST(Placeholder) {
        // Временный тест
        CHECK(true);
    }
}
